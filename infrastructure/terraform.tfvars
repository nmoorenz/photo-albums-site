# Committed. Deployment-specific values are NOT here -- they come from .env
# via scripts/tf.py (bucket_name, domain_name, cognito_domain_prefix,
# aws_profile).

aws_region  = "ap-southeast-6"
project_tag = "photo-albums"
